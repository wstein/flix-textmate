    def f(): Int32 = 1
    pub def g(x: Bool): Bool = if (x) not x else x
    let! y = 1
    forall forA forM foreach
    instance instanceof
    choose choose*
    query solve pquery psolve inject project fix
    select where with from into
    List#{1, 2, 3}
    @Tailrec
    @Terminates
    @MustUseSomedayInvented
    Static Univ static
    let z = 1
